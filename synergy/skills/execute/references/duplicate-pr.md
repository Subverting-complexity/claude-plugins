# Execute — a duplicate PR closes the same issue

Read this only when `wf pr-create`'s result carries `duplicates`: another open pull request already closes the issue this run is about to close.

Report it by number and title. Do not pick the winner or close the other PR here — code review reconciles duplicates, and Phase 10 does not merge while one is flagged.
