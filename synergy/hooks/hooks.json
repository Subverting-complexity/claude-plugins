{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo 'RESPONSE STANDARD: every reply to the user follows the user-facing-communication skill. Open with what was done and the exact current state (committed, pushed, PR opened, reviewed and merged are different). Put anything outstanding, blocked or assumed where it cannot be missed. Name every issue and PR by number and title. Leave out investigation history, file lists and test names.'"
          },
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/repo-host.sh\""
          }
        ]
      }
    ],
    "SubagentStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/repo-host.sh\" SubagentStart"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash|PowerShell|mcp__.*",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/forge-guard.sh\""
          }
        ]
      }
    ]
  }
}
