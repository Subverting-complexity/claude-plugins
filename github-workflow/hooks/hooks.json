{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash(gh issue create*)",
        "hooks": [
          {
            "type": "command",
            "command": "echo WARNING: Prefer /github-workflow:report-issue over raw gh issue create. The skill handles body validation (--body-file), label creation, and Windows shell-escaping. If you are inside the report-issue skill already, proceed."
          }
        ]
      }
    ]
  }
}
